fastapi==0.111.0
uvicorn[standard]==0.30.1
pandas==2.2.2
duckdb==1.1.3
anthropic>=0.111.0   # needs the web_search server tool (Research mode)
apscheduler==3.10.4  # monthly scheduled email reports
matplotlib==3.9.2    # server-side chart rendering for emails
python-dotenv==1.0.1
